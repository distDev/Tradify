import { UIButton } from '@/shared/ui'

interface ExchangeCardFooterProps {}

export function ExchangeCardFooter({}: ExchangeCardFooterProps) {
  return (
    <UIButton className='w-full mt-6' variant='light'>Обментять</UIButton>
  )
}
